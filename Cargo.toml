[package]
name = "generate_update_derive"
version = "0.1.2"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"

[features]
default = ["db", "ts"]
db = []
ts = []
